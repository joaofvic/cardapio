// Placeholder until you run `supabase gen types typescript --project-id <ref> > src/lib/supabase/types.ts`.
// Keeping it permissive lets the app build before the project is provisioned.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
